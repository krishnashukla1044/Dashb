# Environment Configuration for Backend

# Server Port
PORT=3000

# MongoDB Connection URI
MONGO_URI=mongodb://127.0.0.1:27017/dashboard

# JWT Secret Key for Signing Tokens
JWT_SECRET=your_jwt_secret

# Optional: Token Expiration (if you want to control token expiry time)
JWT_EXPIRES_IN=1h

# Optional: Refresh Token Secret (if applicable)
JWT_REFRESH_SECRET=your_jwt_refresh_secret

# Optional: Base URL for API (in case you want a different base URL for production)
BASE_URL=http://localhost:3000

# Optional: Any other configurations (email service, third-party APIs, etc.)
